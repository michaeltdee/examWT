document.getElementById(fetchDataButton).addEventListener('click',);{
    fetch(picsum.photos/400/300?random=$:Math.random(),)
    .then(Response=>Response.json);
//  use .then again to return the data to the data container
// use .catch to handle errors with the api
    Animation(dataContainer,screenLeft);
};
const fetchDataButton = Response.json;
